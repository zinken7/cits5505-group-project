SECRET_KEY=dev-secret-key-change-me
FLASK_ENV=development
DATABASE_URL=sqlite:///watchlist.db
# Bootstrap root user (created once if no User.is_root exists; set strong values in production)
ROOT_USERNAME=root
ROOT_EMAIL=root@localhost
ROOT_PASSWORD=root-root-change-me
